@page
@model TeamEvaluation.Pages.Teams.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Team</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Team.Name)
        </dt>
        <dd class="col-sm-10">
            <a asp-page="./Report" asp-route-id="@Model.Team.Id">@Model.Team.Name</a>
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Team.Members)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Team.Members)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Team.Project)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Team.Project.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Team.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
